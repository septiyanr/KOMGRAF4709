import numpy as np
import math

def translasi_matrix(tx, ty):
    return np.array([[1, 0, tx],
                     [0, 1, ty],
                     [0, 0, 1]])

def rotasi_matrix(theta):
    return np.array([[math.cos(theta), -math.sin(theta), 0],
                     [math.sin(theta), math.cos(theta), 0],
                     [0, 0, 1]])

def scaling_matrix(sx, sy):
    return np.array([[sx, 0, 0],
                     [0, sy, 0],
                     [0, 0, 1]])

def transformasi(p, matrix):
    p_homogeneous = np.array([p[0], p[1], 1])
    hasil_transformasi = np.dot(matrix, p_homogeneous)
    return hasil_transformasi[:2]  


p = np.array([3, 1])


tx = 2
ty = 2
theta = math.radians(45)  
sx = 2
sy = 0.5

matrix_translasi = translasi_matrix(tx, ty)
matrix_rotasi = rotasi_matrix(theta)
matrix_scaling = scaling_matrix(sx, sy)


titik_translasi = transformasi(p, matrix_translasi)
titik_rotasi = transformasi(p, matrix_rotasi)
titik_scaling = transformasi(p, matrix_scaling)


print("Matriks Translasi:")
print(matrix_translasi)
print("\nMatriks Rotasi:")
print(matrix_rotasi)
print("\nMatriks Scaling:")
print(matrix_scaling)


print("\nTitik Awal:", p)
print("Hasil Translasi:", titik_translasi)
print("Hasil Rotasi:", titik_rotasi)
print("Hasil Scaling:", titik_scaling)
